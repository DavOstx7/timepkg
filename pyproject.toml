[tool.poetry]
name = "timepkg"
version = "0.1.0"
description = "Timing Utilities"
authors = ["dav ost <private>"]
license = "LICENSE"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
